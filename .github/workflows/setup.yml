name: Setup CI
on:
  push:
    branches:
      - master
      - main
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Run Upptime
        uses: upptime/uptime-monitor@v1.41.0
        with:
          command: "readme"
      - name: Run Upptime
        uses: upptime/uptime-monitor@v1.41.0
        with:
          command: "site"
      - name: Run Upptime
        uses: upptime/uptime-monitor@v1.41.0
        with:
          command: "graphs"
      - name: Run Upptime
        uses: upptime/uptime-monitor@v1.41.0
        with:
          command: "summary"
